#pragma once

// =================== USER CONFIG =====================================
// Copy this file to "config.h" and fill in your own values.
// "config.h" is listed in .gitignore so it will NOT be committed.
// =====================================================================

const char* WIFI_SSID     = "YOUR_WIFI_SSID";
const char* WIFI_PASSWORD = "YOUR_WIFI_PASSWORD";

const char* PRINTER_IP    = "192.168.1.50";
const char* PRINTER_SERIAL= "0123456789ABCDEF";
const char* ACCESS_CODE   = "12345678";
